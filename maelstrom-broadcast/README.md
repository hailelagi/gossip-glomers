# Multi-Node Broadcast

## Requirements

 run a 5-node cluster for 20 seconds and broadcast messages at the rate of 10 messages per second. It will validate that all values sent by `broadcast` messages are present on all nodes.

## Solution
